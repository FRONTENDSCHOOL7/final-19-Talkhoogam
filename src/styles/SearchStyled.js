import styled from "styled-components";

export const SearchList = styled.article`
  margin: 10px;

  & .user-name {
    font-weight: bold;
  }

  & .user-id {
    font-size: 12px;
    color: var(--color-darkgrey);
  }
`;
